@if($simple)
    <h2 class="fs-20 text-uppercase text-line text-primary my-3">{{ $text }}</h2>
@else
    <h2 class="fs-20 text-uppercase text-white bg-primary w-100 px-3 py-2 rounded my-3">{{ $text }}</h2>
@endif
